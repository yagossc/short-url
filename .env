# This is the sample '.env' configuration file for the
# short-url project. These values are expected to be
# considered sane defaults for a development environment.
#
# If you need to make any changes for developing in your own
# environment, copy this file to '.env.local' and make the
# needed changes there.

# API url to serve as base for the shortened ones
TAPI_URL=http://localhost

# HTTP port to expose the API
TAPI_PORT=8080

# Database driver
TAPI_DB_DRIVER=pgx

# Database URL. Note: change 'localhost' to the correct database location
TAPI_DB_URL=postgres://cloudflare:cloudflare@localhost/cloudflare
